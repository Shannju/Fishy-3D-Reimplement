namespace Fishy.Core
{
    public enum SpeciesId
    {
        Algae,
        Plankton,
        Veg,
        Meat,
        PlayerFish
    }

    public enum FoodType
    {
        Plant,
        Plankton,
        Fish
    }

    public enum ThreatType
    {
        Predator,
        Player
    }
}
